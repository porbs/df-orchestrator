version: '3'
services:
  orchestrator:
    image: "orchestrator:latest"
    ports:
      - "3000:3000"
  worker1:
    image: "worker:latest"
    ports:
        - "3001:3000"
  worker2:
    image: "worker:latest"
    ports:
        - "3002:3000"
  worker3:
    image: "worker:latest"
    ports:
        - "3003:3000"